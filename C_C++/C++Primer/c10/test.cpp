#include <iostream>
#include <vector>
#include <numeric>

using std::cin; using std::cout; using std::endl;
using std::vector; using std::accumulate;

int main() {
    const char *str1 = "abc";
    const char *str2 = "abc";
    cout << (str1 == str2) << endl;
    return 0;
}